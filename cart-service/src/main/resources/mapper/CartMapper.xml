<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.ecommerce.cartservice.mapper.CartMapper">
    <resultMap id="CartResulMap" type="com.company.ecommerce.cartservice.model.Cart">
        <id property="id" column="id"/>
        <result property="status" column="status"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="create_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findActiveCartUserId" resultMap="CartResulMap">
        select id, user_id, status, created_at, updated_at
        from carts
        WHERE user_id = #{userID}
    </select>

    <insert id="insert"
            parameterType="com.company.ecommerce.cartservice.model.Cart"
            useGeneratedKeys="true"
            keyProperty="id">
        insert into carts(
                          user_id,
                          status
        ) values (
                  #{userId},
                  #{status}
                         )
    </insert>

    <update id="updateStatus">
       update carts
       set status = #{status}
       WHERE id = #{id}
    </update>

</mapper>